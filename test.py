from reasoning import detect_intent

print(detect_intent("start the robot"))
print(detect_intent("move forward"))
print(detect_intent("turn left"))
print(detect_intent("pick the box"))
print(detect_intent("show status"))